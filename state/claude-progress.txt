========================================
마지막 업데이트: 2026-04-22 (세션 44 — 사이드바-렌더러 인덱스 불일치 버그 수정)
버전: battery_pack_renderer.html, src/app-state.js, src/app-panel.js, src/app-render.js
테스트 상태: 브라우저 기능 확인 필요
feature_list.json: 확인 필요

========================================
## 세션 44 핵심 성과

### 버그 발견 및 수정: 사이드바-렌더러 인덱스 불일치

증상:
  - 사이드바 "열 우선 L→R (2종)" 클릭 → "비표준 5" (3종) 이미지 표시
  - 사이드바 "비표준 8 (3종)" 클릭 → "열 우선 R→L" (2종) 이미지 표시
  - 이름·m_distinct 레이블과 렌더링 이미지가 2개씩 뒤바뀜

원인:
  - enumerateGroupAssignments 반환 원본 배열 순서:
      [0:열우선L→R(m=2), 1:비표준5(m=3), 2:비표준6, 3:비표준7,
       4:비표준8(m=3), 5:열우선R→L(m=2), 6:보스트로페돈L→R, 7:보스트로페돈R→L]
  - 사이드바는 m_distinct 오름차순 정렬 후 표시:
      [0:열우선L→R, 1:열우선R→L, 2:비표준5, 3:비표준6,
       4:비표준7, 5:비표준8, 6:보스트로페돈L→R, 7:보스트로페돈R→L]
  - selected_ordering = 사이드바 위치 인덱스로 저장
  - 렌더러는 원본 배열의 동일 인덱스를 사용 → 인덱스 불일치!
  - 위치 1 클릭 → 원본[1]=비표준5 렌더링 (열우선R→L이 아님)
  - 위치 5 클릭 → 원본[5]=열우선R→L 렌더링 (비표준8이 아님)

수정 (3파일):
  1. src/app-state.js: `let _sortedCandidates = null;` 변수 추가
  2. src/app-panel.js:
     - _renderCandCards() 에서 _sortedCandidates = candidates 저장
     - _showCandDetail() 에서 _sortedCandidates[idx] 우선 사용
     - _enumResult=null 시 _sortedCandidates=null 함께 리셋
  3. src/app-render.js:
     - effectiveCandidates = _sortedCandidates || ... 우선 사용
     - _enumResult=null 시 _sortedCandidates=null 함께 리셋 (4곳)

========================================
## 이전 세션 요약

세션 41-43:
  - 탐색 시간 제한 UI (1분/10분/30분/1시간)
  - 형상종류/품질 필터 콤보박스
  - 후보 표시 상한 200개
  - CPU 최적화 병렬 탐색 (os.cpus()-2)
  - 커스텀 배열 stagger 열거 버그 수정
  - 필터 갱신 순서 보장

세션 38-40:
  - m_distinct 합판 단위 계산 수정 (plate-level)
  - 커스텀 배열 니켈 플레이트 교차(쇼트) 방지
  - .gitignore 보완

========================================
## 신규/수정 파일

- src/app-state.js: _sortedCandidates 전역 변수 추가
- src/app-panel.js: 인덱스 동기화 수정
- src/app-render.js: 인덱스 동기화 수정
- tests/gen_svgs.js: 신규 (3S2P 후보 SVG 생성 디버그 도구)
- src/enum-worker.js: 신규 (웹워커, 미통합)
- tests/parallel_search.js: CPU 최적화 수정
