========================================
마지막 업데이트: 2026-04-22 (세션 45 — 반복 상한 제거 + 워커 강제 종료 타이머)
버전: battery_pack_renderer.html, src/gen-enum.js, src/app-render.js, src/enum-worker-bundle.js
테스트 상태: 브라우저 기능 확인 완료 (104개 후보 탐색 성공)
feature_list.json: 전체 passes: true (미완료 항목 없음)

========================================
## 세션 45 핵심 성과

### 1. 반복 횟수 상한(5만·200만) + adjStarts 캡(5개) 완전 제거

변경 파일: src/gen-enum.js, src/enum-worker-bundle.js

수정 내용:
  - MAX_ITER_BT = exhaustive ? Infinity : (custom ? 2000000 : 50000)
    → MAX_ITER_BT = Infinity (분기 제거)
  - if (!exhaustive && adjStarts.length >= 5) break;  × 2개 제거
  - max_iter_hit: btIterations >= 50000  →  max_iter_hit: false (고정)
  - enum-worker-bundle.js 재생성 (58.6 KB)

탐색 중단 조건 (최종):
  1. 시간 초과: Date.now() - btStart > btBudgetMs
  2. 탐색 공간 소진: DFS 자연 종료

### 2. 워커 강제 종료 타이머 추가

변경 파일: src/app-render.js

문제: Promise.all에 타임아웃 없음 → 워커가 budget_ms를 조금이라도
      초과하면 UI가 무한 대기
수정: 각 워커에 killer setTimeout(budgetMs + 3000) 추가
      - 정상 응답 시: clearTimeout(killer) → 즉시 resolve
      - 초과 시: w.terminate() → resolve([])

### 3. 탐색 결과 확인 (13S4P 커스텀 배열, 10분)

파라미터: rows=[5,12,13,12,10], offsets=[-1,-2,-2,-2,0]
          B+ 왼쪽, B- 오른쪽, G0앵커=TL, ICC①②ON/③OFF, I형OFF

결과:
  - 총 후보: 104개 (중복 제거 후)
  - m_distinct=12: 1개  ← 최적 (유일)
  - m_distinct=13: 23개
  - m_distinct=14: 80개
  - m_distinct=11: 없음 (구조적 한계)

========================================
## 이전 세션 요약

세션 44:
  - 사이드바-렌더러 인덱스 불일치 버그 수정
  - _sortedCandidates 전역 변수 도입

세션 41-43:
  - 탐색 시간 제한 UI (1분/10분/30분/1시간)
  - 형상종류/품질 필터 콤보박스
  - 후보 표시 상한 200개
  - CPU 최적화 병렬 탐색 (os.cpus()-2)
  - 커스텀 배열 stagger 열거 버그 수정
  - 필터 갱신 순서 보장

세션 38-40:
  - m_distinct 합판 단위 계산 수정 (plate-level)
  - 커스텀 배열 니켈 플레이트 교차(쇼트) 방지
  - .gitignore 보완

========================================
## 신규/수정 파일

- src/gen-enum.js: 반복 상한 제거 (MAX_ITER_BT, adjStarts 캡)
- src/app-render.js: 워커 강제 종료 타이머 추가
- src/enum-worker-bundle.js: 재생성 (58.6 KB)
- src/enum-worker.js: exhaustive: true 고정
- tests/parallel_search.js: g0_anchor TL 추가, exhaustive true
- scripts/build-worker-bundle.js: 번들 생성 스크립트 신규

========================================
## 다음 작업 후보

- feature_list.json 전체 passes: true → 신규 기능 필요 시 사용자 지시 대기
- m_distinct=12 유일 후보를 기준으로 실제 제조 검토 (사용자 판단)
